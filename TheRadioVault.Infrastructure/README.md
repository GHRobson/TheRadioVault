# Radio Vault local infrastructure

This project owns the platform-neutral local database, Research Pack, scanner, preservation, backup and Radio Vault Anywhere infrastructure used by the Avalonia desktop.

It contains no WPF, Windows presentation or Avalonia UI dependencies. The historical shared namespaces are retained for database and package compatibility.
