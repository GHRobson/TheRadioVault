# Radio Vault v0.32.0 Alpha 10 Buildfix 10 — static validation

Build identity: `0.32.0-alpha10-buildfix14-persistence-export-ui-refinement`

## Focused correction scope

- The selected sidebar keyline remains fixed-width but is reduced to 178 pixels for main destinations and 158 pixels for expanded show destinations, preserving its complete right edge beside the scrollbar.
- Library/Favourites, Moments, Now Playing and Research own their headings inside page content; the shared fixed heading strip remains available only to screens that still need it.
- Now Playing uses a main metadata column beside a 350-pixel Up Next rail. The page contains no duplicate seek, skip, play/pause, speed or volume controls.
- Now Playing people and topics use the same grouped `accent-pill` presentation as the Library selected-broadcast panel.
- The persistent player uses a 110-pixel shell row with increased bottom padding, larger Favourite and information actions, and a direct Save Moment command.
- Inactive shared-playback wording is hidden; the transfer prompt remains visible when another device owns playback.
- Dashboard secondary continuation is labelled Up next. On This Day binds researched `People` and `Topics` collections and places Play/Favourite actions beside the broadcast identity.
- No database schema, LAN capability, authoritative-audit import, playback lease or Library grid contract is changed.

## Checks performed

- All Avalonia AXAML and project XML files parse as XML.
- JSON documents parse successfully.
- New bindings resolve to explicit presentation view-model properties and commands.
- The Now Playing page contains no duplicate playback seek/transport/volume controls.
- Source-manifest hashes are reproducible and the packaged ZIP is path-safe and CRC-clean.

A definitive Windows compile, launch and visual check remains required because this packaging environment does not provide the project’s .NET/Avalonia toolchain.
