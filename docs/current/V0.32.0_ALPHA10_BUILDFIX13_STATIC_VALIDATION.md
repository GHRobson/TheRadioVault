# Alpha 10 Buildfix 13 static validation

Build identity: `0.32.0-alpha10-buildfix14-persistence-export-ui-refinement`

This package is a focused seek/resume and shell-chrome correction over the user-confirmed Buildfix 12 compile baseline.

Static checks cover:

- Avalonia AXAML, WPF XAML, project XML and JSON parsing.
- Avalonia event-handler resolution and the persistent playhead gesture contract: tunnel press/release handling, live value capture and pointer-capture-loss commit.
- Project-reference resolution and changed-C# delimiter/string/comment balance.
- Playback identity/generation guards, seek cancellation ownership, post-seek position confirmation, trusted observed-position shutdown flush and explicit completed-state reset semantics.
- Shared Favourite and Moments vector templates across Library, Now Playing and the persistent player.
- Integrated shell contracts: top-reaching sidebar, status-only connection dot, no reserved full-width title strip, and scroll-activated content divider.
- Source-manifest hashes, report identity, ZIP CRC and path safety.

The container does not include the Windows .NET SDK or Windows audio stack, so the definitive compile, launch, drag-seek, shutdown-resume, completed-replay and Connected Access playback tests remain the Windows acceptance gate.
