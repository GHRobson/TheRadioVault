# Alpha 10 Buildfix 12 static validation

Build identity: `0.32.0-alpha10-buildfix14-persistence-export-ui-refinement`

This package is a focused playback-reliability and UI-polish correction over the user-confirmed Buildfix 11 baseline.

Static checks cover:

- Avalonia AXAML, WPF XAML, project XML and JSON parsing.
- Avalonia event-handler resolution, including the playhead press/release lifecycle.
- Project-reference resolution and changed-C# delimiter balance.
- Playback contracts for stale snapshot rejection, real-position shutdown flush, seek buffer rebuilding, remote rewind persistence and completed-remote restart behaviour.
- UI contracts for consistent vector navigation icons, a simplified Moments bookmark, list-only Favourites and compact Library progress.
- Source-manifest hashes, report identity, ZIP CRC and path safety.

The container does not include the Windows .NET SDK or audio stack, so the definitive compile, launch, seek, shutdown-resume and cross-broadcast playback tests remain the Windows acceptance gate.
