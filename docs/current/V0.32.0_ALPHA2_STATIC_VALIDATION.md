# Radio Vault v0.32.0 Alpha 2 — static validation

Build identity: `0.32.0-alpha2-avalonia-design-foundation`

The packaged source was checked for:

- version agreement across `VERSION.txt`, Avalonia and WPF project metadata;
- 45 well-formed XML, XAML, AXAML and project files;
- 17 valid JSON and solution-launch files outside the source manifest;
- 681 packaged source files outside the manifest itself;
- existing project and solution references;
- matching AXAML/XAML `x:Class` implementations;
- retained PowerShell 5.1 validation fixes;
- retained startup-hardening and lifetime/namespace corrections;
- absence of generated `bin`, `obj`, `artifacts` and `.vs` directories;
- absence of unsupported `Grid.Padding` and obsolete `TextBox.Watermark`;
- system-aware light/dark theme dictionaries;
- complete semantic-resource lookup for all 106 design-resource references;
- semantic Radio Vault design resources and reusable control styles;
- selected navigation state in the toolkit-neutral presentation layer;
- binding-property coverage for the redesigned shell, Dashboard and Library;
- loading, empty, favourite, attention and selection-state markers;
- persistent inactive playback-shell markers;
- architecture, WPF-independence and Avalonia-foundation script identities;
- source-manifest hashes and ZIP integrity.

The Windows .NET/Avalonia compile and live visual acceptance remain the authoritative runtime gate on the user's machine.
