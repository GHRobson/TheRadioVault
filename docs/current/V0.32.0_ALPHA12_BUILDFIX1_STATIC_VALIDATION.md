# Radio Vault v0.32.0 Alpha 12 Buildfix 1 — Static validation

Build identity: `0.32.0-alpha12-buildfix1-handoff-health-library-sync`

## Scope checked

- The successfully built Alpha 12 source is retained as the baseline.
- Avalonia server command handling accepts desktop ownership claims and publishes speed/state changes.
- Local-server and remote-client `IPlaybackHandoffService` registrations resolve to real implementations.
- Now Playing shared-device bindings exist independently of the current-broadcast visibility gate.
- Active Avalonia playback reports through the shared ownership service every two seconds.
- Connected Archive Health resolves through the authenticated server API.
- Local and remote `ILibraryMaintenanceService` implementations provide status and scan operations.
- Server scan routing, provider contracts, DTOs and remote-client methods are present.
- File-system watching is restricted to registered roots and supported media, is debounced, and retains an hourly due scan.
- Scan completion invalidates the episode snapshot and emits a full-library federation change.
- Database schema and compatibility identities remain schema 45 / LAN generation 14 / API v1.

## Validation completed in this package environment

- All XAML, AXAML, project, props and targets XML parsed successfully.
- All C# sources passed lexical delimiter, string/comment and merge-conflict-marker checks.
- `IWebArchiveProvider` members are present in the production provider and test fake.
- `ILibraryMaintenanceService` members are present in both local and remote implementations.
- Every new route constant is referenced by server dispatch and remote-client code.
- Playback command, ownership-report and server-claim paths were checked for all server/desktop/phone directions.
- All `DesktopToolsViewModel` construction sites supply Archive Health and Library maintenance dependencies.
- Version identity is consistent across `VERSION.txt`, WPF reference and Avalonia project files.
- Release metadata and source manifest were regenerated after the final source changes.
- Source archive integrity and exclusion of `.git`, `bin`, `obj` and `artifacts` were checked.

## Limitation

The .NET SDK is not installed in this execution environment, so Release compilation and live three-device Windows/iPhone testing were not possible here. Run `BUILD-AND-RUN.cmd` and complete the included acceptance checklist before treating the buildfix as accepted.
