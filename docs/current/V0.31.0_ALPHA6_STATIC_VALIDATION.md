# Radio Vault v0.31.0 Alpha 6 Buildfix 1 static validation

Target: `0.31.0-alpha6-di-tests-cleanup-buildfix1`

The packaged source is checked for:

- valid solution/project/XAML XML;
- matching XAML event handlers;
- required Core Hardening projects and contracts;
- platform-neutral Application/Core boundaries;
- a frozen and validated application composition graph;
- local media creation through `ILocalPlaybackEngineFactory`;
- playback-session creation through `PlaybackSessionFactory`;
- no direct WPF construction of `WpfMediaPlaybackEngine` or `PlaybackSessionCoordinator`;
- remote-session disposal in the ordered shutdown pipeline;
- Alpha 6 composition and lifecycle regression-test markers;
- database schema 45 and LAN capability generation 14;
- sealed source-manifest and ZIP integrity.

The Windows Release build and runtime acceptance pass must still be performed on the user's Windows machine.

## Packaged static result

- 39 XAML/project XML files parsed successfully.
- 394 established XAML event references resolved.
- 11 solution projects were present.
- 278 C# source files passed structural checks.
- 590 source-manifest entries were sealed and verified.
- Architecture report violations: 0.


## Buildfix 1 remote Now Playing guards

- Remote mode exits the local broadcast-knowledge path before `DatabaseService.GetBroadcastKnowledge`.
- Live details load through `LanFederationConnectionService.LoadBroadcastDetailsAsync`.
- Synchronized/cache-safe fallback, server artwork, role-separated people, topics, archive notes, request generation checks and shutdown/mode-switch cancellation are all required markers.
