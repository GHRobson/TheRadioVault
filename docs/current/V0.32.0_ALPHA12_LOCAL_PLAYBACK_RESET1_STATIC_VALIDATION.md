# Alpha 12 Local Playback Reset 1 — static validation

The local-only source package was checked before packaging.

- active Avalonia project dependency closure contains eight local/cross-platform projects and no `TheRadioVault.Web` reference;
- active Avalonia source contains no LAN federation, remote-store, web-server or remote research-pack runtime reference;
- the Avalonia `Remote` directory and Connected Access view are absent;
- all explicit `Compile` and `ProjectReference` paths resolve;
- 53 XML, XAML, AXAML and project files parse;
- 33 JSON files parse before manifest generation;
- changed/new C# files pass lexical delimiter checks;
- no merge-conflict markers, symbolic links, unsafe Windows paths or bundled build-output directories are present;
- the package contains a dedicated local-only solution and a single-project build/release path.

A Windows/.NET compilation is still required by `BUILD-AND-RUN.cmd` because the packaging environment does not provide the .NET 8 SDK or Windows audio runtime.
