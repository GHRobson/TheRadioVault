# v0.32.0 Alpha 1 Buildfix 1 static validation

The packaged-source pass confirmed:

- Avalonia owns `TheRadioVault.exe`;
- WPF is explicitly retained as `TheRadioVault.WpfReference.exe`;
- Avalonia is the first desktop executable in the solution;
- the shared `.slnLaunch` profile starts Avalonia only;
- both no-PowerShell CMD launchers are present;
- project references, XML/AXAML structure and source-manifest hashes are internally consistent;
- schema 45, LAN generation 14 and API/cache identities are unchanged.

Windows compilation and runtime acceptance remain to be performed on the user's machine.
