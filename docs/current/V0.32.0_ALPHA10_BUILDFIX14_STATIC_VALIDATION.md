# Alpha 10 Buildfix 14 static validation

Build identity: `0.32.0-alpha10-buildfix14-persistence-export-ui-refinement`

This package is a focused persistence, Research-export and UI-consistency correction over the user-confirmed Buildfix 13 compile/launch baseline.

Static checks cover:

- Avalonia AXAML, WPF XAML, project XML and JSON parsing.
- Avalonia event-handler resolution, project-reference resolution and changed-C# lexical delimiter/string/comment balance.
- Shutdown ownership: periodic progress writers stop before the final snapshot, the current logical position is frozen, canonical persistence is awaited and the final local/server value is verified.
- Connected Access durability: final encrypted-cache writes are serialized and server-confirmed local mutations are protected from synchronization requests that began before those mutations completed.
- Remote Dashboard/bootstrap summaries are rebuilt from current broadcast rows before cache persistence.
- The centred `Closing RadioVault` overlay and bounded close workflow.
- WPF-style Research `Export pack…` show/year scope selection in local and server-owned sessions.
- Correct line-vector window controls, top-level sidebar branding without the RV tile, consistent navigation-icon centres, glyph-only Dashboard/mini-player actions and list-only Favourites.
- Contextual compact Library favourites and the 17-pixel visible progress bar.
- Source-manifest hashes, report identity/count, ZIP CRC and path safety.

The container does not include the Windows .NET SDK or Windows audio stack. The definitive compile, launch, shutdown-resume and Connected Access persistence tests remain the Windows acceptance gate.
