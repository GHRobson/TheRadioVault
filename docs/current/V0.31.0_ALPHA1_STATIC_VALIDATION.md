# Radio Vault v0.31.0 Alpha 1 static validation

Target: `0.31.0-alpha1-architecture-baseline-buildfix1`

The source package was checked for XML/XAML/project parseability, solution/project-reference consistency, neutral-project Windows dependency violations, presence of all new application contracts and Windows adapters, version consistency, source-manifest integrity and ZIP integrity.

The package deliberately does not change database schema 45, LAN capability generation 14, API v1 or web/cache identities. It cannot prove Windows compilation, runtime WPF adapter loading, live library behaviour or LAN operation in this packaging environment; those checks remain in `V0.31.0_ALPHA1_ACCEPTANCE_CHECKLIST.md`.

Buildfix 1 additionally validates that the WPF application-lifetime adapter uses the fully qualified `global::System.Windows.Application` type so the sibling `TheRadioVault.Application` namespace cannot shadow it.
