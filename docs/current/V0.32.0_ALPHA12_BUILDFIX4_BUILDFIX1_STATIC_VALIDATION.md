# v0.32.0 Alpha 12 Buildfix 4 Buildfix 1 — Static validation

## Corrected build failure

The Avalonia desktop project compiles a curated set of shared source files from the retained WPF project. Buildfix 4 placed the five transactional `IWebArchiveProvider` implementations in `WebArchiveProvider.PlaybackTransfers.cs`, but the Avalonia project linked only `WebArchiveProvider.cs` and `WebArchiveProvider.RemoteAdministration.cs`.

The default shell therefore emitted five CS0535 errors. Buildfix 1 explicitly links the transactional partial class file.

## Checks

- Both `WebArchiveProvider.cs` and `WebArchiveProvider.PlaybackTransfers.cs` resolve from the Avalonia project.
- The effective linked provider source contains all five transactional interface methods.
- All 54 XAML/MSBuild/XML files parse.
- The changed project and documentation files pass structural checks.
- Source manifest and ZIP integrity are regenerated after the correction.

A Windows .NET 8 compile remains the definitive validation.
