# v0.28.0 stable baseline

Current version: `0.28.0`

Accepted implementation baseline: `0.28.0-rc1`, successfully built and tested on desktop and laptop without reported release-blocking issues.

Database schema: 45. Library Truth export schema: 6. Audio files remain untouched.

The final promotion contains no archive-model or database behaviour changes. It updates release metadata/documentation and advances only the secure web shell cache from v24 to v25 so mobile clients replace RC HTML while preserving downloaded audio and artwork caches.
