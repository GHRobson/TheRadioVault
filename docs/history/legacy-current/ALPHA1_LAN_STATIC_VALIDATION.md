# v0.30.0 Alpha 1 Static Validation

Validated target: `0.30.0-alpha1-lan-federation-foundation`

- Project and informational versions match the Alpha 1 identity.
- Assembly/file versions are `0.30.0.0`.
- Database schema remains 45.
- Capability generation is 8.
- PWA shell, audio, artwork and IndexedDB identities are unchanged.
- LAN discovery announces only privacy-safe server metadata.
- Pairing requires HTTPS, a short-lived six-digit code and a stable client identity.
- Remote client authentication accepts a per-client header token without exposing it in URLs.
- WPF Connected access UI exposes server and client-bootstrap workflows.
- MainWindow XAML and all project XML parse successfully.
- Embedded browser and service-worker JavaScript retain valid syntax.
