# Radio Vault v0.30.0 Final Static Validation

Target: `0.30.0`

## Verified in the source package

- Stable version identity is consistent across `VERSION.txt`, WPF project metadata, root documentation and the LAN discovery regression fixture.
- Database schema remains 45 and LAN capability generation remains 14.
- The accepted RC1 application implementation is retained without new API routes, migrations, cache identities or feature surfaces.
- Alpha 9 parity, Research workspace, artwork, encrypted cache and local-database isolation contracts remain present.
- Beta 1 incremental synchronization, deterministic structural resets, copied sync diagnostics, safe mode transitions and bounded shutdown safeguards remain present.
- The deterministic Release gate verifies the built application's ProductVersion against `VERSION.txt`; the source validator now checks the actual two-argument `StartsWith` call used by that guard.
- The build publishes clean self-contained `win-x64` output under `artifacts`.
- Packaging records the stable channel and accepted RC1 baseline, writes `BUILD_INFO.json`, creates a ZIP and emits a SHA-256 checksum.
- Final release notes, stable release boundary and acceptance checklist are present alongside the retained RC1 evidence.
- All 37 XAML/project XML files parse successfully, all 401 XAML event-handler references resolve to C# methods, and the 506-file source manifest, extracted-manifest hashes and ZIP integrity are checked during source packaging.

## Previously verified by the user

The directly preceding RC1 source built successfully and behaved as expected in live testing. The stable source intentionally changes release identity and documentation only.

## Not verified in this environment

The packaging environment does not include the .NET 8 WindowsDesktop SDK, WPF runtime or the user's archive. It cannot prove the final Windows compilation, execution of PowerShell build/package scripts, live LAN behaviour, media playback or existing-install promotion. Those checks are covered by `V0.30.0_FINAL_ACCEPTANCE_CHECKLIST.md`.
