# v0.32.0 Alpha 1 Buildfix 4 static validation

Build identity: `0.32.0-alpha1-buildfix4-avalonia-default-shell`

## Completed in the packaging environment

- Parsed every packaged XML, XAML, AXAML, project and JSON file.
- Confirmed no Avalonia `Grid` carries a `Padding` attribute.
- Confirmed no Avalonia `TextBox` uses the obsolete `Watermark` attribute.
- Confirmed the Buildfix 2 lifetime aliases remain intact.
- Confirmed the Buildfix 3 named startup argument and `global::Avalonia` qualifications remain intact.
- Verified active build, architecture and foundation identities are Buildfix 4.
- Verified solution project paths, project references, source-manifest hashes and ZIP integrity.

## Still required on Windows

The packaging environment does not contain the .NET SDK or Windows desktop toolchain. Visual Studio compilation, full release-gate execution and runtime acceptance therefore remain pending on the user's Windows machine.
