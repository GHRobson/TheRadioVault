# Radio Vault v0.32.0 Alpha 3 — static validation

Build identity: `0.32.0-alpha3-local-playback-rubberbanding`

The packaged-source validation boundary checks:

- Avalonia 12.1.0 and NAudio 2.3.0 package pins;
- Avalonia ownership of `TheRadioVault.exe` and retained WPF reference identity;
- absence of WPF dependencies from Avalonia source;
- toolkit-neutral Presentation project purity;
- `ILocalPlaybackLibraryService`, canonical playback models and service implementation;
- NAudio `IPlaybackEngine` implementation;
- registration through `PlaybackSessionCoordinator` and `PlaybackViewModel`;
- live transport, seek, volume, speed, Now Playing and Play/Resume bindings;
- canonical progress, completion and multipart markers;
- global `ScrollViewer` elastic behaviour, inertia, chaining and reduced-motion markers;
- retained Alpha 1 startup hardening and Alpha 2 design resources;
- XML/XAML/AXAML/project and JSON parseability;
- solution/project-reference integrity;
- source-manifest hashes and ZIP integrity.

The source-generation environment cannot run the Windows .NET/Avalonia compiler or audio backend. `BUILD-AND-RUN.cmd` and the full release gate therefore remain the authoritative Windows compile/runtime checks.
