# Radio Vault v0.32.0 Alpha 10 Buildfix 9 — static validation

Build identity: `0.32.0-alpha10-buildfix14-persistence-export-ui-refinement`

## Focused correction scope

- Main sidebar destinations use a 192-pixel `shell-nav-frame`; child show destinations use a 172-pixel `shell-nav-child-frame`.
- Selected state is owned by the Border `selected` class with a transparent background and `RvAccentBrush` keyline. The former checked ToggleButton navigation surface is absent.
- Year and month archive views show a left-aligned interactive breadcrumb rather than a far-right Back button.
- `ArchiveRootCommand` jumps to years and `ArchiveYearCommand` jumps to the selected year's month grid.
- Month/day lists reuse `SelectedBroadcastDetailsTemplate`, so artwork, play, favourite, full information, summary, people and topics remain visible on the right.
- No Dashboard, Research, playback-handoff, database-schema or LAN capability behaviour is changed.

## Checks performed

- All Avalonia AXAML and project XML files parse as XML.
- JSON documents parse successfully.
- New Library bindings resolve to explicit `LibraryViewModel` properties and commands.
- Navigation source contains no `ToggleButton` with `shell-nav` or `shell-nav-child` classes.
- Source-manifest hashes are reproducible and the packaged ZIP is path-safe and CRC-clean.

A definitive Windows compile, launch and visual check remains required because this packaging environment does not provide the project’s .NET/Avalonia toolchain.
