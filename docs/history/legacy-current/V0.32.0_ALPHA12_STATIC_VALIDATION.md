# Radio Vault v0.32.0 Alpha 12 — Static validation

Build identity: `0.32.0-alpha12-research-coverage-undated-broadcasts-polish`

## Scope checked

- Alpha 11 Buildfix 4 retained as the source baseline.
- Now Playing queue remains in the visual tree when there is no active broadcast.
- Dashboard discovery-card favourite actions are absent from On This Day, Recently Added and Unheard Broadcasts.
- The shared missing-artwork template uses the Radio Vault radio-mast glyph and is referenced by the principal Avalonia artwork surfaces.
- Library list view exposes the `HideCompleted` checkmark and Clear Filters resets it.
- Local and remote `IResearchWorkspaceService` implementations provide undated browsing, protected manual date assignment and show coverage.
- Federation route constants, handlers, provider contracts, remote DTOs and client methods are present for all three Research operations.
- Manual date handling protects `date_confidence='Manual'` from later parser replacement and permits guarded canonical promotion.
- Database schema and capability identities remain unchanged at schema 45 / LAN generation 14 / API v1.

## Validation completed in this package environment

- All XAML, AXAML, project and solution XML files parsed successfully.
- Changed C# files passed lexical delimiter and conflict-marker checks.
- Every `IResearchWorkspaceService` implementation contains the new contract members.
- Every `IWebArchiveProvider` implementation contains the new contract members.
- New federation route constants are referenced by both server dispatch and remote client code.
- Targeted SQLite smoke tests passed for undated enumeration, manual-date propagation and coverage aggregation query shapes.
- Referenced Alpha 12 Avalonia resource keys exist.
- Source archive contents were checked for excluded build-output directories and regenerated into `SOURCE_MANIFEST.sha256.json`.

## Limitation

The .NET SDK is not installed in this execution environment, so Release compilation and live Windows/Avalonia execution were not possible here. The source package therefore requires the normal Windows build-and-run check before acceptance.
