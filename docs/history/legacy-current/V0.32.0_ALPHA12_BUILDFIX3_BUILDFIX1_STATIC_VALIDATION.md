# v0.32.0 Alpha 12 Buildfix 3 Buildfix 1 — Static validation

Build identity: `0.32.0-alpha12-buildfix3-buildfix1-avalonia-handoff-icon-xaml`

## Compile correction

- Replaced `StrokeLineJoin="Round"` with Avalonia-supported `StrokeJoin="Round"` on the contextual handoff `Path` in `MainWindow.axaml`.
- Confirmed no Avalonia AXAML file contains `StrokeLineJoin`, `StrokeStartLineCap` or `StrokeEndLineCap`.
- Confirmed the handoff geometry, `StrokeLineCap` and contextual visibility binding remain unchanged.
- Normalized Research import-change string arguments before provenance recording to remove the four `CS8604` warnings shown in the failed Windows build.

## Regression checks

- All XML/XAML/MSBuild files parse successfully.
- Existing Alpha 12 Buildfix 3 transport, one-second heartbeat and 250 ms projected-playhead markers remain present.
- Source manifest and final ZIP hashes are regenerated after the correction.

## Environment limitation

The packaging environment does not contain the .NET SDK or PowerShell. The exact Windows compiler error has been corrected directly, but the replacement package still requires `BUILD-AND-RUN.cmd` on Windows for definitive compilation.
