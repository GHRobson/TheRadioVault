# Radio Vault v0.32.0 Alpha 4 Buildfix 2 — static validation

Build identity: `0.32.0-alpha4-buildfix2-library-row-polish-responsive-overscroll`

Packaged-source validation covers the accepted Alpha 4 services and pages plus direct-tracking overscroll markers, inertial-tail filtering, the faster single release spring, expandable navigation state, dynamic collection children, collection-scoped `LibraryBrowseRequest` construction, markup/XML parsing, project/solution references, source-manifest hashes and ZIP integrity.

A Windows .NET/Avalonia build and live show-filter/overscroll acceptance pass remain required.

## Buildfix 2 interaction refinements

- Verify overscroll releases without a perceptible dwell after meaningful input stops.
- Verify per-show rows omit the redundant collection subtitle.
- Verify the left playback control appears only on row hover and becomes Pause for the active broadcast.
- Verify dates are bold and split across two lines, the Structure column is absent, and the search glyph is centered.
