# Radio Vault v0.31.0 RC1 static validation

Target: `0.31.0-rc1-core-hardening-release-candidate`

The packaged-source validation requires:

- matching `VERSION.txt`, project `Version` and `InformationalVersion`;
- all retained v0.30 and v0.31 regression markers;
- valid XML, XAML and project files plus XAML event-handler references;
- the Windows PowerShell 5.1-safe validator interpolation and typed-variable fixes;
- architecture validation with zero violations;
- WPF-independence proof with zero hard-boundary violations;
- nine neutral projects with no Windows UI markers and only two intentional Windows boundary projects;
- no direct process, clipboard, Registry, `SystemParameters`, concrete WPF media-engine construction, concrete playback-session construction, direct playback-engine commands or legacy remote-sync ownership in presentation;
- RC1 documentation, report identities, release-channel packaging metadata and Avalonia handoff readiness;
- deterministic Release compilation, console regression tests and compiled ProductVersion verification through `release-gate.ps1`.

The accepted Beta 1 Windows build, full release gate and runtime pass are the promotion baseline. Final packaged-binary and daily-driver acceptance remain Windows runtime checks.
