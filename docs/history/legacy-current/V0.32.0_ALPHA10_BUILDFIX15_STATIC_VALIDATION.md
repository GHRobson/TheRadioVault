# Alpha 10 Buildfix 15 static validation

Build identity: `0.32.0-alpha10-buildfix15-buildfix2-presentation-log-boundary`

This package is Buildfix 15 Buildfix 2: a compile correction over the focused Connected Access progress-compatibility and glyph-treatment correction. The previous Buildfix 1 namespace qualification was not sufficient because `DiagnosticLog.cs`, despite using the `TheRadioVault.Services` namespace, belongs to the WPF shell project and is not compiled into the referenced `TheRadioVault.Services` assembly. `PlaybackViewModel` now uses `System.Diagnostics.Trace` for this non-fatal diagnostic and retains the intended project boundary.

Static checks cover:

- Avalonia AXAML, WPF XAML, project XML and JSON parsing.
- Avalonia event-handler resolution, project-reference resolution and changed-C# lexical delimiter/string/comment balance.
- Presentation-project dependency hygiene: `PlaybackViewModel` contains no reference to the WPF shell-only `DiagnosticLog` type and uses `System.Diagnostics.Trace.WriteLine` instead.
- Direct comparison with the accepted 0.31 client/server progress path: the 0.32-only `claim-device` command is detected as unsupported when the server returns `Unknown playback command`, while the established `offline-progress` mutation remains active.
- Handoff telemetry isolation: current-server ownership conflicts still stop a stale writer, but transient/version-specific handoff failures cannot suppress the canonical progress save.
- Buildfix 14 shutdown guarantees remain present: periodic writers stop, the live logical position is frozen, the server-confirmed mutation and encrypted cache flush are awaited, and the Closing RadioVault overlay remains visible.
- Glyph-only controls override the Fluent template content presenter in normal, pointer-over and pressed states so no pale hover tile is painted.
- Dashboard discovery and Library-row play actions use the same unframed blue vector language as the sidebar Now Playing icon; active Library playback uses a matching blue pause glyph.
- Source-manifest hashes, report identity/count, ZIP CRC and path safety.

The container does not include the Windows .NET SDK or Windows audio stack. The definitive compile, launch and 0.31-server progress-persistence tests remain the Windows acceptance gate.
