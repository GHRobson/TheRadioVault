# Radio Vault v0.31.0 Alpha 2 static validation

Target: `0.31.0-alpha2-application-service-extraction`

The source package was checked without executing the Windows application in this environment.

Validated areas:

- version identity alignment across `VERSION.txt`, project metadata and release tooling;
- platform neutrality of `TheRadioVault.Application`;
- explicit Windows adapter registration in the WPF composition root;
- startup policy extraction and live `MainWindow` integration;
- shutdown ordering, failure isolation, watchdog retention and final-progress ordering;
- duplicate-safe local/remote window-transition integration;
- absence of the former WPF-owned `RunShutdownStep` helper;
- retention of database schema 45, LAN capability generation 14 and existing web/cache identities;
- focused application-service regression test markers;
- XAML/XML/project parsing, event-handler references, source-manifest verification and ZIP integrity.

Actual Windows Release compilation and live acceptance remain required on the user's machine.

Package-level results recorded for this source archive:

- **39** XAML/project XML files parsed successfully;
- **393** detected XAML event-handler references resolved;
- **11** solution projects and all project references were present;
- **550** source-manifest entries were generated and verified;
- the Application project contained no Windows UI markers;
- the architecture report recorded zero boundary violations.
