# v0.32.0 Alpha 12 Buildfix 3 Buildfix 2 — Static validation

Build identity: `0.32.0-alpha12-buildfix3-buildfix2-handoff-takeover-loading-feedback`

## Handoff repair

- The Avalonia web playback controller routes ownership claims through `PlaybackViewModel.ReleaseForRemoteHandoff`, which checks and pauses the playback engine directly.
- The laptop saves any outgoing local state before reserving the new session, preventing an old heartbeat from consuming the pending claim.
- The takeover claim is sent before remote audio preparation and the shared provider retains the pending claim for 45 seconds.
- `LoadAndPlayCoreAsync` supports an already-reserved claim without issuing a duplicate command or replacing the requested playing state with the server's newly paused state.
- The first live report is followed by `WaitForCurrentDeviceOwnershipAsync`; an unconfirmed takeover pauses local output and reports an error.
- The remote handoff adapter rejects unchanged claim results instead of treating them as success.

## Loading feedback

- `IsPrimaryTransportLoading` covers ordinary media preparation and the complete ownership-transfer operation.
- The persistent transport button hides Play/Pause/Move glyphs and shows an animated four-frame activity glyph while loading.
- Research pack preview and apply operations have separate busy states, indeterminate progress indicators, working button labels and a dispatcher render yield before heavy work begins.

## Regression checks

- All 54 XML/XAML/MSBuild files parse successfully.
- All 378 C# files pass lexical/structural balance checks, and Avalonia markup contains no WPF-only stroke properties.
- Existing one-second authoritative heartbeat and 250 ms projected-playhead paths remain present.
- Database schema, LAN capability generation and API identities are unchanged.
- The source manifest tracks 843 files and the final ZIP is re-opened, integrity-tested and re-hashed after packaging.

## Environment limitation

The packaging environment does not contain the .NET SDK or PowerShell. Structural and contract validation can be completed here, but definitive Windows compilation still requires `BUILD-AND-RUN.cmd`.
