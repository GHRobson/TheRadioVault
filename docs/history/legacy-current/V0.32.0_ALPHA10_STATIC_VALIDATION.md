# Radio Vault v0.32.0 Alpha 10 — static validation

Build identity: `0.32.0-alpha10-buildfix15-buildfix2-presentation-log-boundary`

This package was validated in a non-Windows environment. Validation covers source structure, markup/JSON parsing, project and linked-source references, modified C# delimiter structure, required UI/Research/handoff markers, authoritative-import SQL behaviour retained from the accepted Buildfix 6 baseline, source-manifest hashes, Windows/OneDrive-safe paths and ZIP integrity.

Validated package contents:

- 795 source-manifest files.
- 53 XML/XAML/AXAML/project files parsed successfully.
- 34 JSON files and the solution-launch file parsed successfully.
- 37 project references resolved.
- The two modified C# presentation files passed lexical delimiter checks; the remaining 361 C# sources are unchanged from the accepted Buildfix 6 package.
- Every AXAML event reference, including the new title-bar drag and window-control handlers, resolves to its code-behind method.
- Avalonia 12 window chrome uses `WindowDecorations="BorderOnly"`, client-area extension and explicit minimise, maximise/restore, close and drag behaviour.
- Connected Access status now sits beside the integrated window controls rather than in the page-heading strip.
- Dashboard alone collapses the fixed page heading; its title and description are present inside the scrolling Dashboard content.
- The focused Buildfix 6 Dashboard composition remains present: five-to-three continuation/stats split, WPF-inspired Surprise Me panel, explicit On This Day topic area, and matching Recently Added/Unheard columns.
- Dashboard statistic icons have no circular containers and use four independent yellow, blue, green and pink vector resources.
- Dark surfaces use the revised blue/slate hierarchy while Radio Vault yellow remains the primary action accent in both themes.
- Shared card frames, Dashboard row frames, Library row/period/detail framing and the active-playing edge remain present in Avalonia markup.
- The WPF/server authoritative importer, remote preview contract and Avalonia local/remote import paths retain the explicit `authoritative_audit` policy.
- Server-owned playback ownership checks and stale-writer protection remain present.
- The longest path under the short `RadioVault` package root is 94 characters.

This validation cannot replace the Windows .NET/Avalonia compile, launch test, visual review or live three-device acceptance matrix. Run `BUILD-AND-RUN.cmd` from a clean extraction.


## Buildfix 8 focused Library validation

- Confirmed the fixed-width yellow-keyline navigation selectors, shared Library list frame, reordered row actions, direct information controls, grouped people pills and square year/month WrapPanel tiles are present in packaged source.
- Confirmed the accepted Buildfix 7 custom chrome and Dashboard markers remain present.
## Buildfix 9 Library navigation correction

- Sidebar main and child destinations use fixed-width selected Border keylines rather than checked ToggleButton surfaces.
- Archive period and month/day views expose left-aligned All, year and month breadcrumb controls.
- The year breadcrumb returns directly to the month grid; All returns directly to the year grid.
- Month/day drill-down uses the shared selected-broadcast details template beside the framed list.
- Buildfix 8 Library composition and Buildfix 7 Dashboard/custom chrome remain preserved.

