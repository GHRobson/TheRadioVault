# Radio Vault v0.31.0 Alpha 5 static validation

Target: `0.31.0-alpha5-lan-shared-session-consolidation`

The packaged validation boundary checks:

- all solution/project XML and XAML are well formed;
- XAML event handlers resolve to code-behind methods;
- neutral projects remain free of Windows UI dependencies;
- `RemoteLibrarySessionCoordinator` and its neutral session models exist;
- `MainWindow` resolves and uses the shared coordinator for cached adoption, synchronization leases, cursor advancement, failure transitions, timer eligibility, reconnect and closing;
- the old WPF-local synchronization semaphore and cancellation-source fields do not return;
- regression coverage exercises cursor ownership, duplicate-sync suppression, failure backoff, cached read-only state and manual retry;
- database schema 45 and LAN capability generation 14 remain unchanged;
- the sealed source manifest and extracted ZIP contents verify.

Windows compilation and live local/server/remote-client acceptance remain required on the target machine.
