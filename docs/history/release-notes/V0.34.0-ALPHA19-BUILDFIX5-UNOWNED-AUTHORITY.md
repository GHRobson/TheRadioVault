# Radio Vault 0.34.0 Alpha 19 Buildfix 5

Buildfix 4 successfully opened the canonical recording, started audible playback inside the iPhone tap and advanced the decoder beyond one second. The server then rejected the proof because its private playback authority retained a `Server` sentinel even while the public session correctly reported owner `None` and contained no source episode.

The transfer coordinator now identifies a genuinely empty source by the facts that it has no episode and is not playing. In that state an audibly running, gesture-authorized phone decoder is valid proof. Any real paused or playing source episode continues to require the normal muted preparation and source-stop boundary.

Install the Buildfix 5 server, fully close Radio Vault Web, reopen it and confirm Buildfix 5 before starting playback directly from the phone Library. The matching desktop client keeps connected capability generation 27 aligned.
